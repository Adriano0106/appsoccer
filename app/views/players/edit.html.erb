<div class="centered">
    <h1><%= t('player.edit') %></h1>

    <%= render 'form', player: @player %>

    <%= link_to t('show'), @player, class:"btn btn-info" %>
    <%= link_to t('back'), players_path, class:"btn btn-secondary" %>
</div>
